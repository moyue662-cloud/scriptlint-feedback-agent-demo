# DecisionPatch dependencies
# Python 3.11+ (tested on 3.13.15)

# --- Web UI ---
streamlit>=1.36,<2.0

# --- Data models / validation ---
pydantic>=2.7,<3.0

# --- LLM provider (七牛云 OpenAI 兼容接口) ---
openai>=1.35,<2.0

# --- Video audio review (PyAV bundles FFmpeg libraries) ---
faster-whisper>=1.2,<2.0
numpy>=1.26,<3.0

# --- Storage ---
# sqlite3 is in the standard library; no extra package needed.

# --- Evaluation / testing ---
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0

# --- Optional: lightweight embeddings for memory retrieval (P1 fallback) ---
# numpy is pulled in by other deps; keep optional to avoid heavy install.
# numpy>=1.26
