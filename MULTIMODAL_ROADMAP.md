# ScriptLint：从剧本审计到多模态视频审片

## 当前可交付边界

黑客松版本只承诺分析用户上传或粘贴的剧本文本，完成四件事：

1. 保存用户自己的项目、集数和剧本版本；
2. 提取动作能力、外观、知情边界、具体台词归属、情绪情境、道具与身份等带行号事实；
3. 将用户改稿意见拆成候选规则，逐条确认后才进入项目记忆；
4. 在后续版本自动检索这些规则，并同时展示“改稿原话”和“剧本证据”。

确定性解析器只识别当前声明支持的中文模式。识别不到时返回空结果，不以猜测代替证据。

## 为什么现在不直接做视频

视频审片至少还需要镜头切分、ASR、字幕对齐、说话人识别、角色跟踪、服化道状态识别、动作与表情识别。八天内把这些都放进 Demo，会让“反馈如何变成记忆、第二次如何准确生效”这个赛题核心变得不稳定且难评测。

因此当前策略是先稳定审计核心，再逐步增加证据来源。

## 已经铺好的接口

`schemas/multimodal.py` 定义了稳定的五层对象：

- `ReviewAsset`：剧本、字幕、音频或视频资产；
- `ReviewEvidence`：一条可回看的原始证据；
- `EvidenceLocator`：文本行号、时间码、帧号和画面区域；
- `ReviewObservation`：从任意模态提取出的“角色—动作—对象—值”事实；
- `ReviewModality`：`script_text / subtitle / audio / video_frame`。

当前 `services/review_pipeline.py` 已把文本事实转换为上述统一观察对象。未来模型适配器只负责产生同样的对象；项目规则、确认门禁、冲突审计和结果解释无需重写。

```text
剧本文本 ─ 文本提取器 ─┐
字幕文件 ─ 字幕解析器 ─┤
视频音轨 ─ ASR/说话人 ─┼─ ReviewObservation ─ 规则记忆 + 一致性审计 ─ 可回放报告
视频画面 ─ 角色/状态识别 ┘
```

## 黑客松后四阶段

### 阶段 A：字幕与时间码（约 1 周）

- 导入 SRT/VTT；
- 将台词、角色与剧本行进行对齐；
- 报告可以点击跳到字幕时间码；
- 先验证“错人说台词”和“角色说出不应知道的信息”。

验收：20 段人工标注字幕中，说话人/知情边界问题的证据时间码可复核。

### 阶段 B：视频基础管线（约 2 周）

- 视频上传、转码、镜头切分与关键帧抽取；
- ASR 与字幕时间轴；
- 角色登记照和镜头内角色匹配；
- 报告支持视频片段回放。

验收：每个发现能定位到片段，而不是只输出一段模型文字。

### 阶段 C：服化道与动作状态（约 2–4 周）

- 角色服装、伤口位置、主要道具状态；
- 简单动作和左右手使用；
- 同一角色跨镜头状态轨迹；
- 低置信度观察必须人工确认。

验收：使用自建的短片对照集，分别统计召回率、误报率和人工复核时间。

### 阶段 D：跨模态审片（约 2–4 周）

- 剧本要求与成片画面对照；
- 台词、动作、表情和道具联合判断；
- 用户修正某次识别后，形成可复用项目规则或识别偏好；
- 形成“剧本行—字幕时间码—视频帧—用户反馈”的完整证据链。

验收：同一项目第二次审片时，已确认反馈能稳定改变结果，且不泄漏到其他项目。

## 数据与评测原则

- 固定人工样例用于可重复回归，不冒充真实行业数据；
- 用户自有剧本用于产品可用性验证，导入前应脱敏；
- 视频阶段先用自摄或已获授权素材，避免版权与隐私风险；
- 分别统计提取准确率、规则应用准确率、误报率、延迟和 token/模型成本；
- 任何低置信度视觉结论都显示“待确认”，不自动写入长期记忆。
