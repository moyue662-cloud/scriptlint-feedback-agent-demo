# ScriptLint：从剧本审计到多模态视频审片

## 当前可交付边界

黑客松版本已经完成“文本审计 + 视频音频审片”的纵向切片：

1. 上传 MP4/MOV/MKV/WebM/AVI/M4V 成片并临时处理；
2. 从视频容器提取 16kHz 单声道音轨，检查低音量、削波和高静音比例；
3. 使用 faster-whisper 生成中文时间码转写，与“角色：台词”格式的剧本逐行对齐；
4. 报告疑似漏词、改词、加词，并保留剧本行与音频时间码；
5. 保存用户自己的项目、剧本版本和编导确认过的反馈规则，在后续版本复用。

确定性解析器只识别当前声明支持的中文模式。识别不到时返回空结果，不以猜测代替证据。

## 当前没有假装解决的问题

当前已经能接收视频并审计音轨，但音频本身无法可靠证明“是谁说的”，更不能判断动作、表情、服装、伤口和道具。说话人识别、镜头切分、角色跟踪和视觉状态识别仍属于后续阶段；ASR 异常也必须由编导回看确认。

## 已经铺好的接口

`schemas/multimodal.py` 定义了稳定的五层对象：

- `ReviewAsset`：剧本、字幕、音频或视频资产；
- `ReviewEvidence`：一条可回看的原始证据；
- `EvidenceLocator`：文本行号、时间码、帧号和画面区域；
- `ReviewObservation`：从任意模态提取出的“角色—动作—对象—值”事实；
- `ReviewModality`：`script_text / subtitle / audio / video_frame`。

当前 `services/review_pipeline.py` 已把文本事实转换为统一观察对象，`services/audio_review_service.py` 已实现视频音轨、ASR 时间码和台词对齐报告。未来视觉模型只需补充帧级观察；项目规则、确认门禁和结果解释无需推倒重写。

```text
剧本文本 ─ 文本提取器 ─┐
字幕文件 ─ 字幕解析器 ─┤
视频音轨 ─ ASR/说话人 ─┼─ ReviewObservation ─ 规则记忆 + 一致性审计 ─ 可回放报告
视频画面 ─ 角色/状态识别 ┘
```

## 黑客松后四阶段

### 已完成：视频音轨与时间码 MVP

- 视频上传与临时文件清理；
- PyAV 音轨解码，不依赖系统安装 FFmpeg；
- faster-whisper 中文 ASR；
- 剧本台词与 ASR 全局字符对齐；
- 漏词、改词、加词、低音量、削波与静音比例提示。

已完成工程验收：MP4/AAC → WAV 实际解码测试、Whisper tiny 实际模型加载与推理、纯函数对齐回归测试。行业准确率尚未验证。

### 下一阶段 A：字幕、人工修订与说话人（约 1–2 周）

- 导入/导出 SRT、VTT；
- 人工修订 ASR 后重新对齐；
- 说话人分离与角色人工映射；
- 点击问题直接定位视频时间码。

验收：20 段人工标注素材中，台词差异与说话人结果均能回到可复核时间码。

### 下一阶段 B：镜头与视觉状态（约 2–4 周）

- 角色服装、伤口位置、主要道具状态；
- 简单动作和左右手使用；
- 同一角色跨镜头状态轨迹；
- 低置信度观察必须人工确认。

验收：使用自建的短片对照集，分别统计召回率、误报率和人工复核时间。

### 下一阶段 C：跨模态审片（约 2–4 周）

- 剧本要求与成片画面对照；
- 台词、动作、表情和道具联合判断；
- 用户修正某次识别后，形成可复用项目规则或识别偏好；
- 形成“剧本行—字幕时间码—视频帧—用户反馈”的完整证据链。

验收：同一项目第二次审片时，已确认反馈能稳定改变结果，且不泄漏到其他项目。

## 数据与评测原则

- 固定人工样例用于可重复回归，不冒充真实行业数据；
- 用户自有剧本用于产品可用性验证，导入前应脱敏；
- 视频阶段先用自摄或已获授权素材，避免版权与隐私风险；
- 分别统计提取准确率、规则应用准确率、误报率、延迟和 token/模型成本；
- 任何低置信度视觉结论都显示“待确认”，不自动写入长期记忆。
