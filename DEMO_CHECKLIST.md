# ScriptLint 现场演示检查单

## 演示前 30 分钟

- [ ] 切到 `codex/scriptlint-demo` 分支；
- [ ] 运行 `py -B -m pytest -q -p no:cacheprovider`；
- [ ] 运行 `py -m streamlit run app.py`；
- [ ] 点击“重置完整演示”；
- [ ] 确认侧栏显示“本地 SQLite / 确定性规则引擎 / Schema 校验开启”；
- [ ] 完整走一遍：首次审计 → 生成候选 → 确认并复审 → 后续相似任务 → 评测；
- [ ] 浏览器缩放保持 100%，关闭无关标签页和通知；
- [ ] 不在屏幕上展示 API Key、真实剧本或参与者身份。

## 三分钟现场路径

1. 审计台：无记忆时 0 个问题；
2. 纠正：生成 candidate；
3. 确认：出现 1 个违例和双向证据；
4. 后续任务：第 2 集再次命中；
5. 评测：展示双组对照与隔离；
6. 用户验证：说明真实数据仍在收集，不伪造结果。

## 无网与故障兜底

页面无法运行时：

```powershell
py -B scriptlint_phase2_demo.py
py -B eval\run_scriptlint_eval.py
```

两个命令都不需要网络或 API Key。

如果评测页临时失败，只展示已经当场运行得到的命令行输出；不要口头补造数字。

## 演示后

- [ ] 记录评委追问；
- [ ] 将真实用户测试只录入“用户验证”页；
- [ ] 导出匿名 CSV 并保留日期；
- [ ] 将失败案例转成新的回归测试，再修改代码。
